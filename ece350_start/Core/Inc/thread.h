/*
 * thread.h
 *
 *  Created on: May 19, 2024
 *      Author: Ethan Romero
 */

#ifndef INC_THREAD_H_
#define INC_THREAD_H_

//#include <stdint.h>
//
//extern unsigned int numThreads;
//// Array to store pointers to the top of each thread stack. (aka last value pushed to stack)
//extern uint32_t* p_threadStacks[];
//
//uint32_t* Create_Thread_Stack(); // Each thread should have its own stack.
//void print_continuously(void);
//void Init_Thread_Stack(uint32_t** p_threadStack, void (*callback)());
//
///*
// * brief: Given some nth thread, we use this function to determine its starting stack address. Assumes all thread stacks are of same size.
// */
//uint32_t* Get_Thread_Stack(unsigned int threadNum);

#endif /* INC_THREAD_H_ */
